<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_1"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".userview"></activity>
        <activity android:name=".findrestaurant"></activity>
        <activity android:name=".login"></activity>
        <activity android:name=".enterestaurant"></activity>
        <activity android:name=".waiting"></activity>
        <activity android:name=".takeout"></activity>
        <activity android:name=".choose"></activity>
        <activity android:name=".checkout"></activity>
        <activity android:name=".done"></activity>
        <activity android:name=".selectitem"></activity>
    </application>

</manifest>